STREAMAX_PROJECT_DIR=/opt/streamax
BACKUP_PATH=/srv/streamax-backups
BACKUP_KEEP=7
# Optional: configure this remote with rclone before enabling offsite copies.
# BACKUP_REMOTE=remote:streamax-backups
